<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:StudyFlowSA.Views"
       x:Class="StudyFlowSA.AppShell"
       Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent Title="Home" Route="home" ContentTemplate="{DataTemplate views:HomePage}" />
        <ShellContent Title="Tasks" Route="tasks" ContentTemplate="{DataTemplate views:TasksPage}" />
        <ShellContent Title="Calendar" Route="calendar" ContentTemplate="{DataTemplate views:CalendarPage}" />
        <ShellContent Title="Progress" Route="progress" ContentTemplate="{DataTemplate views:ProgressPage}" />
    </TabBar>

</Shell>